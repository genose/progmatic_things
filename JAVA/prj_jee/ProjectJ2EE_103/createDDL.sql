CREATE TABLE Adresse (IDADRESSE INTEGER NOT NULL, CODEPOSTAL VARCHAR(255), RUE VARCHAR(255), TYPE INTEGER, VILLE VARCHAR(255), PRIMARY KEY (IDADRESSE))
CREATE TABLE personne (ID INTEGER NOT NULL, NOM VARCHAR(255) NOT NULL, PRENOM VARCHAR(255) NOT NULL, AGE INTEGER, PRIMARY KEY (ID, NOM, PRENOM))
CREATE TABLE personne_Adresse (ID INTEGER NOT NULL, NOM VARCHAR(255) NOT NULL, PRENOM VARCHAR(255) NOT NULL, adresses_IDADRESSE INTEGER NOT NULL, PRIMARY KEY (ID, NOM, PRENOM, adresses_IDADRESSE))
ALTER TABLE personne_Adresse ADD CONSTRAINT FK_personne_Adresse_ID FOREIGN KEY (ID, NOM, PRENOM) REFERENCES personne (ID, NOM, PRENOM)
ALTER TABLE personne_Adresse ADD CONSTRAINT FK_personne_Adresse_adresses_IDADRESSE FOREIGN KEY (adresses_IDADRESSE) REFERENCES Adresse (IDADRESSE)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
